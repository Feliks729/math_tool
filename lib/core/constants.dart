// ============================================
// ФАЙЛ: lib/core/constants.dart
// ЧАСТЬ ПЛАНА: 1.1 — Константы проекта
// ЧТО ДЕЛАЕТ: единое место для всех констант — сетка, цвета, математика
// ЗАВИСИТ ОТ: —
// ============================================

import 'package:flutter/material.dart';

// ─── Сетка ───────────────────────────────────────────────────────────────────

/// Размер одной клетки сетки в логических пикселях
const double kCellSize = 40.0;

/// Минимальный масштаб (отдалить)
const double kZoomMin = 0.1;

/// Максимальный масштаб (приблизить)
const double kZoomMax = 20.0;

/// Масштаб по умолчанию
const double kZoomDefault = 1.0;

// ─── Цвета ───────────────────────────────────────────────────────────────────

/// Цвет линий сетки
const Color kColorGrid = Color(0xFFDDDDDD);

/// Цвет осей координат
const Color kColorAxis = Color(0xFF888888);

/// Цвет точки по умолчанию
const Color kColorPoint = Color(0xFF2962FF);

/// Цвет выделенного объекта
const Color kColorSelection = Color(0xFFFF6D00);

/// Цвет фона поля
const Color kColorBackground = Color(0xFFFFFFFF);

// ─── Математика ──────────────────────────────────────────────────────────────

/// Точность сравнения вещественных чисел (для проверки эквивалентности)
const double kEpsilon = 1e-10;

/// Шаг по оси X при построении графика функции
const double kPlotStep = 0.05;

/// Количество контрольных точек для числовой проверки эквивалентности выражений
const int kEquivalenceCheckPoints = 6;
